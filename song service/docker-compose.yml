version: "3.8"
services:
    cache:
        image: redis/redis-stack:latest
        container_name: redis-musicService-spotify
        ports:
            - "6379:6379" # Standard Redis
            - "8001:8001" # Redis Insight (GUI)
        # No need for 'app' service because my app is running locally
